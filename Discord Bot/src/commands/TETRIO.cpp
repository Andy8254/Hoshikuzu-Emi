#include "core/CommandRegistry.hpp"

/*
The following section deals with data retrived from TETR.IO
using curl/curl.h and TETR.IO's TETRA Channel API. The core
feature is shared through <tetrio_base.hpp> and <tetrio_base
.cpp> for sharable use in other tournament-related modules.
*/

void register_tetrio_commands(dpp::cluster& bot) {

}