# /tournament bracket match_svg
Exports the selected match as SVG.
## Usage
`/tournament bracket match_svg id:<id> match_id:<match_id>`
## Details
Use when staff need a compact visual for the selected match.
## Notes
This only creates visual output. It does not change the selected match state.
