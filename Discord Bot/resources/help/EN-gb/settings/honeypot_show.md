# /settings honeypot_show
Shows the current honeypot channel status.
## Usage
`/settings honeypot_show`
## Details
Displays the configured honeypot channel, or reports that no honeypot channel is armed.
## Notes
Use this command to audit whether the automatic-ban trap is currently active.
