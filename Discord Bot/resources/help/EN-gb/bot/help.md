# /bot help
Shows the help browser.
## Usage
`/bot help [category] [command]`
## Details
Use `category` to open a module page such as `tournament` or `settings`. Add `command` to open a command-specific page.
## Notes
Localized help falls back to `EN-gb` when a page is missing or intentionally empty.
