# /tournament register
Registers or unregisters a player by slash command.

## Usage
`/tournament register id:<id> [username] [abort] [user]`

## Details
Players can register themselves; staff can target another user for manual recovery.

## Notes
Prefer the registration panel for normal player flow. Use `abort:true` to unregister.
