# /tournament checkin
Checks in or unchecks a player by slash command.

## Usage
`/tournament checkin id:<id> [username] [abort] [user]`

## Details
Players can check themselves in; staff can target another user if panel flow fails.

## Notes
Prefer the check-in panel for normal player flow. Use `abort:true` to undo check-in.
