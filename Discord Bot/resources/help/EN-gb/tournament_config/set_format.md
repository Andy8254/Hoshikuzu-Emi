# /tournament config set_format
Sets a tournament default format.

## Usage
`/tournament config set_format id:<id> format:<format>`

## Details
Choose single elimination, double elimination, round robin, or Swiss before generation.

## Notes
Changing format does not automatically rebuild existing matches.
