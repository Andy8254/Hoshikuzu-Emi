# /tournament bracket generate
Generates a bracket from checked-in players.
## Usage
`/tournament bracket generate id:<tournament_id> type:<format>`
## Formats
Single elimination and double elimination are supported for generation.
## Notes
If type is omitted, the tournament's configured format is used.
