# /tetrio
Displays a TETR.IO profile embed.
## Usage
`/tetrio username:<name>` or `/tetrio` to use your linked TETR.IO account.
## Notes
Use `/link platform:tetrio id:<username>` first if you want the username option to be optional.
