# /tournament register
Registers or unregisters a player for a tournament.
## Usage
`/tournament register id:<tournament_id> username:<name>`
## Unregister
`/tournament register id:<tournament_id> abort:true`
## Staff
Staff can add `user:@player` to register or unregister another player.
## Notes
Player-facing use should happen in the configured tournament channel.
